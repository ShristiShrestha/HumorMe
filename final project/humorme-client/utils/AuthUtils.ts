export enum AuthUserRole {
    CONTROL = "CONTROL",
    EXPERIMENT = "EXPERIMENT",
}

export enum AuthUserRoleStatus {
    PENDING = "PENDING",
    AVAILABLE = "AVAILABLE",
    ASSIGNED = "ASSIGNED",
}
