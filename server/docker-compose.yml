services:
  db:
    image: postgres:latest
    restart: always
    secrets:
      - db-password
    volumes:
      - db-data:/var/lib/postgresql/data
#    networks:
#      - humorme-api-net
    environment:
      - POSTGRES_DB_URL=db
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
#    expose:
#      - 5432
    ports:
      - "5432:5432"
#  backend:
#    build: .
#    ports:
#      - 8080:8080
#    networks:
#      - humorme-api-net
volumes:
  db-data:
secrets:
  db-password:
    file: password.txt
#networks:
#  humorme-api-net:
